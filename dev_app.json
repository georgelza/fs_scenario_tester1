{
    "echoConfig": 0,
    "debuglevel": 3,
    "echojson": 0,                      # Echo output JSON to terminal
    "call_fs_api": 1,                   # Make the call to the FS Payment API endpoint
    "httpposturl": "https://dev-env.bankservafrica.prod.risk-hub.io:8443/events",
    "cert_dir": "certs",                # Directory where we will store the certs
    "cert_file": "client.crt",
    "cert_key": "client.key",
    "eventtype": "paymentRT",           # paymentRT or NRT
    "json_from_file": 0,                # Read events from JSON Files
    "input_path": "json_rt_source1",    # Input directory where events/scenario's are stored                  
    "json_to_file": 1,                  # do we output created even and engineresponse to file system          
    "output_path": "json_rt_output1",   # where to write output to
    "testsize": 1,                      # when we running in generate fake data, how many records to we wanna generate
    "sleep": 2000,                      # Milliseconds, aka 5000 => 5 seconds. this mean we will sleep between records between 0 and 5000  
    "MinTransactionValue": 100,         # Whats the low end of the transaction value to generate
    "MaxTransactionValue": 1000,        # Whats the upper limit of the transaction value to generate
    "SeedFile": "seed.json",            # File containing seed data
    "EchoSeed": 0,                      # do we want to echo the see data to the console
    "IntroBadEntity": 0,
    "IntroBadPayer": 0,
    "IntroBadAgent": 0
}

